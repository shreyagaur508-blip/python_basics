list = [1,2,3,4,5]

list.append(6) # This will add the element 6 to the end of the list 'list', making it [1, 2, 3, 4, 5, 6]
print(list)  # This will print the updated list 'list', which is [1, 2, 3, 4, 5, 6]

list.insert(2, 6) # This will insert the element 6 at index 2 of the list 'list', making it [1, 2, 6, 3, 4, 5, 3]
print(list)  # This will print the updated list 'list', which is [1, 2, 6, 3, 4, 5, 3]

list.remove(3) # This will remove the first occurrence of the element 3 from the list 'list', making it [1, 2, 6, 4, 5, 3]
print(list)  # This will print the updated list 'list', which is [1, 2, 6, 4, 5, 3]

list.pop(2) # This will remove and return the last element of the list 'list', which is 3, making it [1, 2, 6, 4, 5]
print(list)  # This will print the updated list 'list', which is [1, 2, 6, 4, 5]

list.sort() # This will sort the elements of the list 'list' in ascending order, making it [1, 2, 4, 5, 6]
print(list)  # This will print the updated list 'list', which is [1, 2, 4, 5, 6]

list.reverse() # This will reverse the order of the elements in the list 'list', making it [6, 5, 4, 2, 1]
print(list)  # This will print the updated list 'list', which is [6, 5, 4, 2, 1]

list.clear() # This will remove all elements from the list 'list', making it an empty list []
print(list)  # This will print the updated list 'list', which is []

list.count(2) # This will return the number of occurrences of the element 2 in the list 'list', which is 1
print(list)  # This will print the result, which is 1

list.copy() # This will create a shallow copy of the list 'list', which is [6, 5, 4, 2, 1]
print(list)  # This will print the copied list, which is [6, 5, 4, 2, 1]

list.extend([7, 8, 9]) # This will add the elements 7, 8, and 9 to the end of the list 'list', making it [6, 5, 4, 2, 1, 7, 8, 9]
print(list)  # This will print the updated list 'list', which is [6, 5, 4, 2, 1, 7, 8, 9]

list.insert(3, 10) # This will insert the element 10 at index 3 of the list 'list', making it [6, 5, 4, 10, 2, 1, 7, 8, 9]
print(list)  # This will print the updated list 'list', which is [6,
