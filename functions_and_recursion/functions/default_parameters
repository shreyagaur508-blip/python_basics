def prod(a =1, b = 1):
    product = a*b
    print("The product of", a, "and", b, "is:", product)
    return product

prod() #function call with default parameters

prod(5, 6) #function call with arguments